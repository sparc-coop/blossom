@implements IDisposable

@code {
    protected override void OnInitialized()
    {
        RootScope.OnStateChanged += StateHasChanged;
    }

    public void Dispose()
    {
        RootScope.OnStateChanged -= StateHasChanged;
    }
}